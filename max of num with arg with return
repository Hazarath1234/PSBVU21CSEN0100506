#include <stdio.h>
int max(int a, int b)
{
    if(a>b)
    {
       return a;
    }
    else
    {
       return b;
    }
}
int main()
{
    int a,b,c,d;
    printf("enter the numbers a and b");
    scanf("%d%d",&a,&b);
    d=max(a,b);
    printf("the max is %d",d);
}          
