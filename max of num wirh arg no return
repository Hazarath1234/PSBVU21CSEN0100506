#include <stdio.h>
int max(int i, int j)
{
    if(i>j)
    {
        printf("%d is max number",i);
    }
    else
    {
        printf("%d is max number",j);
    }
}
int main()
{
    int a,b;
    printf("enter the numbers a and b");
    scanf("%d%d",&a,&b);
    max(a,b);
}
